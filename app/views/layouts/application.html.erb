<!DOCTYPE html>
<html>
<head>

  <title><%= title %></title>

  <link rel="icon" type="image.png" href="assets/logo_16.png"/>
  <link rel="apple-touch-icon" href="assets/logo_110_white.png"/>
  <link rel="apple-touch-startup-image" href="assets/logo_wait.png">
  <link rel="apple-touch-startup-image" href="assets/logo_wait_ipad_square.png">
  <%= javascript_include_tag "application" %>
  <%= stylesheet_link_tag "application" %>
  <script>
  var a=document.getElementsByTagName("a");
for(var i=0;i<a.length;i++)
{
    a[i].onclick=function()
    {
        window.location=this.getAttribute("href");
        return false
    }
}
</script>
  <%= csrf_meta_tags %>
	<%= render 'layouts/stylesheets' %>
	<%= render 'layouts/google_analytics' %>
	



</head>
<body>
    <div class="container">
      <%= render 'layouts/header' %>

      <section class="round">
        <% flash.each do |key, value| %>
          <div class="flash <%= key %>"><%= value %></div>
        <% end %>
    <%= yield %>

      </section>
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
<script>
$(document).ready(function() {
  $("a.fancybox").fancybox();
});
</script>
  </body>
</html>
